# @ydant/portal

## 0.3.0

### New Package

Portal rendering for Ydant — render children into a different target node.

- `portal()` spell for rendering into alternate DOM targets
- `createPortalPlugin()`: Processes portal requests with proper cleanup
- Portal plugin uses `Request` type with `isTagged` narrowing
